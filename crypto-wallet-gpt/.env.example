# Database (DigitalOcean PostgreSQL)
DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require

# Redis (DigitalOcean Valkey)
REDIS_URL=redis://user:password@host:port

# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=# Generate with: openssl rand -base64 32

# Google OAuth
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# Google Cloud KMS
GOOGLE_KMS_PROJECT=your-project-id
GOOGLE_KMS_LOCATION=global
GOOGLE_KMS_KEYRING=wallet-keyring
GOOGLE_KMS_KEY=wallet-key
GOOGLE_APPLICATION_CREDENTIALS_JSON=# Service account JSON as single-line string

# Blockchain (Infura)
INFURA_PROJECT_ID=your-project-id
INFURA_API_KEY=your-api-key
ETHEREUM_NETWORK=sepolia

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development
